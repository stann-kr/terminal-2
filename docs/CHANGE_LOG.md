# 변경 이력

공개 가능한 결과 중심 변경 요약이다. 항목은 일자 단위이며 최신 항목을 위에 둔다.

## 2026-09-08 — 이벤트 탐색과 신청 안정성

- 기본 진입을 이벤트 개요로 바꾸고 Boot/Sleep을 선택형 체험으로 제공한다. Home/Gate는 실제 포스터·일정·장소·신청 상태를 먼저 보여준다.
- LIVE·예정·지난 이벤트 선택과 KST 날짜를 통일하고 잘못된 달력 날짜를 거부한다. 신청 기간과 행사 시작 경계에서 화면 상태를 재평가한다.
- Gate API는 화면 eventId를 서버 대상과 비교한다. 대상이 바뀌면 초안을 보존하고 행사·코드를 재확인한다.
- Transmit의 늦은 성공 응답이 새 초안을 지우지 않도록 수정하고 공개 게시 범위를 안내한다. 언어·별칭·방문 기록의 저장소 접근 실패를 허용한다.
- 공통 언어 제어·본문 크기·폼 위계와 Lineup 키보드 펼침을 정리하고 기능 화면의 반복 효과를 줄였다. Status는 조회 실패를 0건으로 표시하지 않는다.

## 2026-08-23 — Gate capability와 접근성 경계 정리

### Added

- `0000`부터 연속된 SQL, journal tag, snapshot chain과 exact path + SHA-256 lock을 검사하는 migration history guard를 CI에 추가했다.
- Drizzle 생성을 임시 디렉터리에서 검증하고 journal + 다음 SQL + snapshot만 반영한 뒤 lock을 갱신하는 명시적 `db:generate -- --name <safe_name>` wrapper를 추가했다.

### Changed

- Gate의 신청 기간·upcoming event·access-code rule과 D1 atomic repository를 분리하고 Request·code-info route가 같은 domain 판정을 사용하도록 정리했다.
- Gate Request의 지원되지 않던 기타 초대자 입력을 제거하고, 서버가 확인한 아티스트 identity만 읽기 전용으로 표시하도록 변경했다.
- 세 공개 폼의 field error와 첫 오류 focus 계약을 공유하고, 제목이 있는 Terminal panel을 labelled section과 heading으로 렌더링한다.
- Docker build context에서 local env·private 문서·generated output을 제외하고, ESLint·Vitest가 source와 실제 test discovery를 기준으로 실패하도록 설정을 정리했다.
- 사용하지 않는 정적 이벤트 사본·boot context·legacy CRT/accent alias와 과거 텍스트 측정 의존성을 제거했다.
- repository migration history를 `0000`–`0009`의 10개로 정합화하고, `0009`에서 `transmit_logs.created_at`을 지원되는 레거시 값에서 ISO timestamp `TEXT NOT NULL`로 재구성하도록 고정했다. development/production remote apply는 별도 승인·검증으로 유지한다.

### Fixed

- Gate Request와 Signal의 신규·중복 성공 응답을 동일한 `200 { ok: true }`와 `no-store`로 맞춰 제출 결과가 HTTP status로 구분되지 않게 했다.
- `TerminalButton`이 `aria-pressed` 등 native button 속성을 실제 DOM에 전달하도록 수정했다.

## 2026-08-14 — Cloudflare development/production 환경 분리

### Changed

- Wrangler development/production 환경을 고정 Worker와 분리 D1 binding으로 명시하고 build·preview·deploy command가 항상 target environment를 지정하도록 변경했다.
- Cloudflare Workers Builds를 유일한 자동 배포 경로로 정리하고 GitHub Actions는 validation만 담당하도록 경계를 분리했다.
- `dev`는 development Worker, `main`은 production Worker를 대상으로 하며 production deploy와 D1 migration·secret·binding·route 변경은 독립 승인 단계로 유지한다.

## 2026-08-13 — 접근성·공개 API·성능·검증 계층 강화

### Added

- route별 metadata, skip link와 고유 main landmark, Node/jsdom 테스트 project, Next·Worker HTTP smoke, 임시 D1 migration smoke와 secret 없는 validation CI를 추가했다.
- 공개 이벤트/아티스트 runtime decoder, 공통 rate-limit/Turnstile 검증 인터페이스, PII-safe log와 Transmit idempotency 계약을 추가했다.
- Gate와 Lineup 선택 상태를 URL query와 browser history에 동기화했다.

### Changed

- 의미 텍스트는 서버 HTML부터 최종 문자열로 렌더링하고 cipher는 대표 타이틀로 제한했다.
- 전역 WebGL을 Home hero 조건부 dynamic enhancement로 격리하고 Status의 3D realtime 표시는 접근 가능한 정적 레지스트리로 바꿨다.
- Home 이벤트 CTA·semantic directory와 즉시 사용 가능한 intro skip을 추가하고, 근거 없는 build/uptime/realtime 표기를 제거했다.
- 공개 민감 응답을 no-store로 통일하고 게스트 정원 누락을 fail-closed로 처리했다. Signal·Gate 중복 제출은 식별 정보를 노출하지 않는 성공 응답을 사용한다.
- ordinary Docker dev 환경에서 Cloudflare 배포 자격 증명 전달을 제거했다.

### Fixed

- 신청/코드 검증의 loading·invalid·inactive·unavailable 상태를 분리하고 세 공개 폼의 label, field error, live region과 오류 focus를 복구했다.
- reduced-motion/save-data/hidden 상태에서 비필수 timer·scramble·Canvas가 계속 동작하던 문제를 정리했다.

## 2026-08-11 — 공개 API와 런타임 신뢰 경계 정비

### Security

- `use-scramble`의 텍스트 전용 DOM 쓰기 패치를 일반 npm 설치와 Docker 설치 모두에서 적용·검증하도록 고정했다.
- 콘텐츠 보안 정책과 프레이밍 차단 등 기본 보안 응답 헤더를 추가하고 프레임워크 식별 헤더를 제거했다.
- 공개 JSON API가 입력 타입과 허용 필드, 본문 크기를 데이터베이스 접근 전에 검증하도록 강화했다.
- 게스트 신청의 이메일 중복·아티스트별 정원 판정과 선택적 마케팅 등록을 하나의 D1 배치로 처리하도록 고정했다.
- 인증 코드는 URL에 남지 않는 POST 본문으로만 확인하고 응답 캐시를 금지했다.
- 전송 로그 공개 응답과 신규 저장 입력에서 디바이스 식별자를 제외했다.

### Changed

- TypeScript 오류를 무시하던 프로덕션 빌드 설정을 제거했다.
- 로컬 개발과 Docker의 기본 포트를 3005로 통일했다.
- Node 환경의 Vitest 실행 계약을 추가했다.
- 종료 시각이 지난 `UPCOMING` 이벤트를 응답 시점에 보관 상태로 판정하고, 미래 이벤트는 시작 시각순으로 제공한다.

### Fixed

- 동시 구독 요청이 이메일 고유 제약에서 500으로 바뀌던 경쟁 조건을 409 중복 응답으로 안정화했다.
- 인증 코드 연속 입력에서 이전 응답이 최신 입력 상태를 덮어쓸 수 있던 요청 경쟁을 차단했다.

## 2026-07-28 — 공개·비공개 문서 레이어 정리

### Docs

- 공개 문서와 내부 작업 문서의 분리 기준을 적용했다.
- 변경 이력과 트러블슈팅을 일자 단위 항목으로 정리했다.

## 2026-07-23 — 릴리스 정비와 이벤트 수명주기 처리

### Added

- API 응답에 JSON 유효성 가드와 안정적인 식별자를 도입했다.
- 종료된 이벤트의 수명주기를 상태에 따라 처리하도록 했다.

### Changed

- 릴리스 런타임 의존성을 갱신했다.

### Fixed

- 폼 라벨과 디코딩 텍스트의 접근성 이름을 개선했다.

## 2026-06-25 — lint 경고 정리

### Changed

- 누적된 lint 경고를 정리하고 저장소 구성을 정돈했다.

## 2026-06-16 — lint 게이트 복구와 토큰 드리프트 가드

### Added

- 빌드 전 단계에서 공통 디자인 토큰의 해시를 검증하는 드리프트 가드를 도입했다.

### Fixed

- 프레임워크 major 전환으로 무력화됐던 lint 게이트를 flat config 마이그레이션으로 복구했다.

## 2026-06-13 — 부트 스킵과 접근성 글루

### Added

- 부트 시퀀스 건너뛰기를 추가했다.
- 선택·포커스 글루 토큰과 모션 축소 설정 가드를 적용했다.

## 2026-06-12 — STANN OS LIVE 표면 정렬

### Added

- 공통 토큰 정본을 도입했다. 비색상 토큰만 소비하고 색상은 이벤트 스킨을 유지한다.
- 라벨용 모노스페이스 글루를 도입하고 타이틀 폰트는 유지했다.
- 세 표면을 상호 연결하는 SIGNAL_NET 링크와 HUB 진입점을 추가했다.

### Changed

- 페이지 헤더 라벨 문법과 자간 토큰을 정렬했다.

## 2026-05-09 — 지난 이벤트 레이블 처리

### Fixed

- 홈 이벤트 레이블이 시각 경과 후 지난 이벤트 표기로 전환되도록 했다.

## 2026-05-08 — 게스트 신청 기간 경계 보정

### Fixed

- 게스트 신청을 이벤트 시작 시각까지 허용하도록 기간 판정을 보정했다.

## 2026-04-25 — 모바일 장식선 반응형 보정

### Fixed

- 홈 타이틀 장식선과 초대장 구분선이 모바일에서 깨지던 문제를 반응형 여백과 감지 로직 강화로 해결했다.

## 2026-04-24 — SIGNAL 구독 페이지와 타임존 표시

### Added

- 소식 구독 페이지를 신설하고 동의 UI를 컴포넌트로 분리했다.

### Changed

- 전송 로그 타임스탬프를 방문자의 로컬 타임존 기준으로 표시하도록 했다.
- 홈 모듈 카운트를 구독 페이지 추가에 맞춰 갱신했다.

### Fixed

- 디코딩 텍스트가 모바일에서 잘리던 문제를 해결했다.

## 2026-04-22 — 라인업 등록과 시드 데이터 정리

### Added

- 신규 이벤트 라인업을 등록하고 게스트 신청 흐름을 개선했다.

### Security

- 시드 데이터가 이력에 남지 않도록 제거하고 무시 정책을 반영했다.

## 2026-04-21 — 인증 코드 기반 게스트 신청과 상태 대시보드

### Added

- DJ별 인증 코드와 게스트 정원 시스템을 도입했다.
- 인증 코드로 초대인이 자동 완성되도록 하고 폼 순서를 개선했다.
- 게이트 페이지에 이벤트 소개글과 포스터 표시를 추가했다.

### Changed

- 상태 페이지를 실제 이벤트 데이터 기반 대시보드로 전면 교체했다.
- 이벤트 소개글을 줄 단위 디코딩 애니메이션으로 표현했다.

### Fixed

- 전송 로그의 멀티라인 메시지가 잘리던 문제를 해결했다.
- 모바일 입력 포커스 시 화면이 확대되던 동작을 막았다.
- 페이지 헤더 강조 효과, 복귀 링크 접근성, 상태 아이콘 중복, 라인업 컬럼 줄바꿈을 정리했다.

## 2026-04-20 — 폰트 토큰 일관화와 신청 폼 확장

### Added

- 인스타그램 입력란에 고정 접두어와 안내 문구를 추가했다.
- 게스트 신청 폼에 초대인 선택과 코드 사전입력 링크 지원을 추가했다.
- 선택 항목인 마케팅 수신 동의를 추가했다.
- 서버 상태 캐싱 라이브러리를 도입해 모바일 뒤로가기 동작을 최적화했다.

### Changed

- 전 페이지의 임의 폰트 크기를 토큰으로 치환해 일관화했다.
- 영문 번역을 훅 기반 구조로 통합 관리하도록 바꿨다.

### Fixed

- 모바일 반응형 레이아웃을 안정화했다.

## 2026-04-19 — 폰트 토큰 시스템과 공통 폼 컴포넌트

### Added

- 폰트 크기를 CSS 변수와 유틸리티 토큰으로 정의했다.
- 공통 폼 필드 컴포넌트와 입력 스타일 상수를 신설했다.

### Changed

- 신청·전송 폼과 공통 UI 컴포넌트를 토큰 기반으로 이전했다.

## 2026-04-17 — 렌더링 안정화

### Fixed

- 개발 모드 이중 렌더링으로 생기던 디코딩 텍스트 깜빡임을 해결했다.
- 텍스트 스크램블 라이브러리의 HTML 삽입 방식을 텍스트 기반으로 패치해 프로덕션 렌더링 버그를 잡았다.
- 크기 관찰자 피드백 루프를 제거했다.
- 모바일 반응형과 레이아웃을 통일했다.

## 2026-04-15 — 코드 검증과 타이포그래피 안정화

### Fixed

- 보안, 타입, 접근성, 성능 관점의 검증을 반영하고 빌드 오류를 해결했다.
- 타이포그래피 시스템을 최종 안정화했다.

## 2026-04-14 — 라인업 아코디언과 타이포그래피 시스템

### Added

- 라인업에 아티스트 소개 아코디언과 다국어 표시를 추가했다.
- 전역 시맨틱 타이포그래피 시스템을 구축했다.

### Changed

- CRT 래퍼를 디자인 토큰 기반으로 리팩토링했다.

### Fixed

- 디코딩 애니메이션 지연 중 깜빡임을 막고 순차 타이핑 효과를 개선했다.

## 2026-04-13 — 폼 상호작용 보정

### Added

- 전송 로그 페이지네이션의 높이 전환을 자연스럽게 개선했다.

### Fixed

- 모바일에서 버튼 텍스트 디코딩 시 높이가 튀는 문제를 해결했다.
- 전송 중 버튼 상태와 클라이언트 제출 가드를 공통 컴포넌트로 정리했다.

## 2026-04-11 — 다국어 지원과 폰트 적용

### Added

- 언어 컨텍스트와 언어 토글을 도입하고 전 페이지에 한국어·영어 번역을 적용했다.
- 부트 시퀀스에 언어 선택 단계를 통합했다.
- 높이 애니메이션 컴포넌트를 추가했다.
- 타이틀·본문용 로컬 폰트를 적용했다.

### Fixed

- 신청 페이지의 잘못된 태그 중첩으로 발생하던 하이드레이션 오류를 해결했다.

## 2026-04-10 — Cloudflare 배포 인프라와 프레임워크 업그레이드

### Added

- Cloudflare Workers와 D1 기반 배포 인프라 및 수동 deploy workflow를 구성했다.
- 노드 식별자 자동 부여, 게이트 접근 요청 페이지, 카운트다운, 전송 로그 페이지네이션을 추가했다.

### Changed

- 이벤트·아티스트 스키마를 식별자와 JSON 데이터 구조로 유연화했다.
- Next.js를 16 계열로 업그레이드하고 Worker 어댑터 호환성을 맞췄다.
- 컨테이너 베이스 이미지를 런타임 호환성에 맞게 교체했다.

### Fixed

- 엣지 런타임 설정과 번들링 충돌, 미들웨어 설정 누락 등 배포 초기 빌드 문제를 정리했다.
- 부트 시퀀스와 슬립 모드의 레이아웃을 통일했다.

## 2026-04-09 — 디자인 시스템 통합

### Added

- 텍스트 길이 기반 디코딩 애니메이션을 추가했다.
- 시맨틱 텍스트 컴포넌트를 도입하고 애니메이션을 토큰화했다.

### Changed

- 전역 디자인 시스템을 통합하고 하드코딩된 값을 제거했다.
- 폰트 로딩을 최적화하고 파티클 배경과 페이지 전환 효과를 개선했다.

### Fixed

- 라인업 보더 색상과 레이아웃 가로 크기를 보정했다.

## 2026-04-08 — 초기 구축

### Added

- 터미널 컨셉의 이벤트 사이트 초기 구조를 구성했다.
- 전역 텍스트 복호화 전환 시스템을 도입했다.

### Fixed

- 레이아웃 확장 방향과 페이지 전환을 안정화했다.
- 화면 켜짐 효과와 파티클 배경의 포지셔닝 문제를 정리했다.
